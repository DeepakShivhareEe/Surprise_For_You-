{
  "version": 2,
  "buildCommand": "cd version-b && npm install && npm run build",
  "outputDirectory": "version-b/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
